ReplayBufferDock="Replay Buffer Slider"
MainReplayBuffer="Replay Buffer"
Unbound="(unbound)"
PendingDurationTooltip="This buffer is currently active. The new duration will apply once it stops, so it isn't reset."
