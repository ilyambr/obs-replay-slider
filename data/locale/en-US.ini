ReplayBufferDock="Replay Buffer Slider"
MainReplayBuffer="Replay Buffer"
Unbound="(unbound)"
PendingDurationTooltip="This buffer is currently active. Click Apply to restart it with the new duration now, or it will apply on its own once the buffer stops."
Apply="Apply"
